{
  "name": "luvus-toolkit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Account limits, reset countdowns and local agent statistics for Luvus",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "test": "node --test features/ai-usage/test/*.test.mjs features/keep-awake/test.mjs tests/*.test.mjs"
  },
  "dependencies": {
    "@github/copilot-sdk": "1.0.13"
  }
}
